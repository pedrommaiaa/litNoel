---
export interface Props { items: Array<{ href?: string; label: string }> }
const { items } = Astro.props;
---
<nav class="text-sm text-gray-600 mb-4">
  {items.map((b, i) => (
    <>
      {b.href ? <a href={b.href} class="hover:text-red-700">{b.label}</a> : <span>{b.label}</span>}
      {i < items.length - 1 && <span class="mx-2">›</span>}
    </>
  ))}
</nav>


