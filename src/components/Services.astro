---
import { companyData } from '../data/company';
---

<section id="services" class="section-padding bg-gray-50">
  <div class="container-max">
    <div class="text-center mb-12">
      <h2 class="heading-lg mb-4">🎄 Holiday Lighting Services</h2>
      <p class="text-base text-gray-600 max-w-2xl mx-auto">
        From design to takedown, we handle everything so you can enjoy the magic of the holidays stress-free. Explore our comprehensive Christmas lighting solutions below.
      </p>
    </div>

    <!-- Service Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-12">
      {companyData.services.map(service => (
        <div class="card-feature text-center">
          <div class="text-4xl mb-4">{service.icon}</div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">{service.name}</h3>
          <p class="text-gray-600 text-sm leading-relaxed">{service.description}</p>
        </div>
      ))}
    </div>

    <!-- Single CTA -->
    <div class="text-center">
      <p class="text-gray-600 mb-6">
        Ready to light up your holidays? Get your free design consultation today!
      </p>
      <a href="#contact" class="btn-primary">
        🎄 Get Free Design Consultation
      </a>
    </div>
  </div>
</section> 