<section class="py-16 section-mobile-compact bg-white" id="process">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl md:text-4xl font-bold text-center mb-6 text-gray-900">How It Works</h2>
    <div class="max-w-3xl mx-auto divide-y divide-gray-200 bg-white rounded-lg shadow">
      <div class="mb-6">
        <picture>
          <source srcset="/photos/photo-3-800.webp 800w, /photos/photo-3-1200.webp 1200w, /photos/photo-3-1600.webp 1600w, /photos/photo-3-2000.webp 2000w" type="image/webp" />
          <img src="/photos/photo-3-1200.webp" alt="Warm‑white palm wraps and roofline supporting example" class="w-full h-56 object-cover rounded-t-lg" loading="lazy" width="1200" height="800" />
        </picture>
      </div>
      {[
        [
          'Consult & Design',
          'Free on‑site or virtual consultation. We review the property, your requirements, and budget; propose a design; and provide a clear, itemized estimate.'
        ],
        [
          'Professional Install',
          'Scheduled installation by insured technicians. Commercial‑grade LEDs, outdoor‑rated hardware, clean cable routing, and timers. Safe ladder/roof work. Site left clean.'
        ],
        [
          'In‑Season Maintenance',
          'Support included. If a strand fails or shifts, contact us. Typical response time is 24–48 hours.'
        ],
        [
          'Takedown',
          'Post‑season removal on a scheduled date. We remove all materials, coil and pack, and confirm the site is clear.'
        ],
      ].map(([title, body], i) => (
        <details class={`group p-5 ${i===0 ? 'open' : ''}`}>
          <summary class="flex cursor-pointer list-none items-center justify-between text-left">
            <span class="flex items-center text-lg font-semibold text-gray-900">
              <span class="mr-3 inline-flex items-center justify-center w-8 h-8 rounded-full bg-red-600 text-white text-sm font-bold">{i+1}</span>
              {title}
            </span>
            <span class="ml-4 text-gray-500 group-open:rotate-180 transition-transform">⌄</span>
          </summary>
          <div class="mt-3 text-gray-700 leading-relaxed">{body}</div>
        </details>
      ))}
    </div>
    <div class="text-center mt-10">
      <a href="#contact" class="inline-block bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg text-base font-semibold transition-colors">
        Check Availability
      </a>
    </div>
  </div>
</section>


